{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "preDeployCommand": "python scripts/run_migration.py",
    "healthcheckPath": "/health",
    "restartPolicyType": "on_failure"
  }
}
